/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: [
      "lh3.googleusercontent.com",
      "upload.wikimedia.org",
      "s.gravatar.com",
    ],
  },
};

module.exports = nextConfig;
